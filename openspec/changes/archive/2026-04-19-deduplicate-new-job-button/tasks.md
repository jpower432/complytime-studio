## 1. Remove Duplicate Button

- [x] 1.1 Remove the `<button>` from the empty-state block in `JobsView` (`jobs-view.tsx` line ~34)
- [x] 1.2 Keep the empty-state `<h3>` and `<p>` as informational copy only

## 2. Sync Specs

- [x] 2.1 Sync modified `job-lifecycle` spec to `openspec/specs/job-lifecycle/spec.md`
